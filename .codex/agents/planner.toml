name = "planner"
description = "Planning specialist for complex features, architectural changes, and refactoring."
sandbox_mode = "read-only"
developer_instructions = """
Before starting, read `.claude/agents/planner.md` completely. Treat the Markdown body after its YAML frontmatter as the authoritative shared instructions for this role.

Ignore Claude-specific `model` and `tools` frontmatter fields. Stay read-only, analyze the repository, and return a concrete implementation plan unless the parent agent explicitly assigns a narrower planning task.
"""
